# Daily Repo Tracker - 2026-05-24

**Window**: 2026-05-24T00:00:00+08:00 → 2026-05-25T00:00:00+08:00 (Asia/Shanghai) | **Repo**: ethereum-optimism/optimism

## 概览
| 指标 | 数值 |
|------|------|
| Draft PRs | 0 |
| Open PRs | 0 |
| Merged PRs | 6 |
| Closed (未合并) | 0 |
| Key PRs | 6 |
| Commits | 6 |
| Releases | 1 (op-proposer/v1.16.3) |

## Key PRs

### 1. #20997 chore(op-supervisor): delete everything except packages still imported externally [merged]
- **作者**: ajsutton | **规模**: +0/-18,051, 74 files | **标签**: S-on-merge-train
- **内容理解**: 大规模删除 op-supervisor 代码，仅保留仍被外部导入的包（`cross`、`reads`、`superevents`、`types`）。这是将 op-supervisor 功能迁移到 op-supernode 架构的关键步骤。删除了 18,051 行代码和 74 个文件，是一次重大的架构清理。基于 #20994 堆叠构建。
- **关注点**: 🌉 跨链/互操作性 — op-supervisor 是 OP Stack 跨链消息传递的核心组件，此次重构反映 Optimism 正在重新设计互操作架构。

### 2. #20993 chore(interopgen): move depset package to op-chain-ops/interopgen/config [merged]
- **作者**: ajsutton | **规模**: +78/-70, 22 files
- **内容理解**: 将 `op-supervisor/supervisor/backend/depset` 包迁移到 `op-chain-ops/interopgen/config`，Go 包名从 `depset` 重命名为 `config`。该包包含 `FullConfigSet`、`StaticRollupConfigSet`、`DependencySet` 等配置值类型。此举是 op-supervisor 清理的前置操作。
- **关注点**: 🌉 跨链/互操作性 — 依赖集配置迁移

### 3. #20992 chore(op-node): remove unused interop subcommand [merged]
- **作者**: ajsutton | **规模**: +0/-306, 2 files
- **内容理解**: 移除 `op-node interop dev-setup` 和 `op-node interop devkey` 子命令。这些命令在仓库中无任何脚本、Makefile、CI 工作流调用。E2E 测试框架通过进程内调用 `interopgen.Deploy` 而非 shell 方式。属于 interop 架构清理的一部分。
- **关注点**: 🌉 跨链/互操作性 — 清理废弃的 interop 命令

### 4. #20994 chore: remove non-Go op-supervisor build and architecture-doc references [merged]
- **作者**: ajsutton | **规模**: +3/-56, 7 files
- **内容理解**: 从构建/发布流水线和架构文档中移除 op-supervisor 相关引用。包括 justfile 的 TEST_PKGS、docker-bake.hcl 的构建目标、Docker 镜像配置等。标志着 op-supervisor 作为独立可部署组件的终结。

### 5. #20976 feat(op-supernode): add top-level --dependency-set flag [merged]
- **作者**: ajsutton | **规模**: +96/-1, 5 files
- **内容理解**: 为 op-supernode 添加顶层 `--dependency-set` flag（环境变量 `OP_SUPERNODE_DEPENDENCY_SET`）。依赖集是 supernode 管理的所有链的全局属性，之前只能通过 per-VN 的 `--interop.dependency-set` flag 访问。此 flag 加载一次后覆盖每个 VN 配置。
- **关注点**: 🌉 跨链/互操作性 — supernode 全局依赖集管理

### 6. #20999 ci(rust): publish kona prestates on kona-client/v* tag push [merged]
- **作者**: pauldowman | **规模**: +18/-0, 1 file
- **内容理解**: 新增 `kona-publish-prestates-on-tag` 工作流，在 `kona-client/v*` tag push 时上传 hash-named kona prestates 到 GCS。复用现有 `kona-publish-prestate-artifacts` matrix job，与现有 op-program tag-publish 流程对齐。

## PR 状态分布

### Merged (6)
| # | 标题 | 作者 | 规模 |
|---|------|------|------|
| #20997 | chore(op-supervisor): delete everything except packages still imported externally | ajsutton | +0/-18051 |
| #20993 | chore(interopgen): move depset package to op-chain-ops/interopgen/config | ajsutton | +78/-70 |
| #20992 | chore(op-node): remove unused interop subcommand | ajsutton | +0/-306 |
| #20994 | chore: remove non-Go op-supervisor build and architecture-doc references | ajsutton | +3/-56 |
| #20976 | feat(op-supernode): add top-level --dependency-set flag | ajsutton | +96/-1 |
| #20999 | ci(rust): publish kona prestates on kona-client/v* tag push | pauldowman | +18/-0 |

### Releases
- **op-proposer/v1.16.3** (2026-05-23T21:32:18Z)

## 趋势信号
- **op-supervisor → op-supernode 架构迁移**: ajsutton 主导的一系列 PR 清晰表明 Optimism 正在将 op-supervisor 功能整合到 op-supernode 中。当日删除了 ~18,000 行 op-supervisor 代码，仅保留外部依赖的包。这是 OP Stack 互操作架构的重大重构。
- **Kona (Rust fault proof client)**: 新增 tag-push 触发的 prestate 发布工作流，表明 Kona 客户端正在走向更稳定的发布流程。
